import { Meta, Story, Props } from '@storybook/addon-docs';
import { boolean, select } from '@storybook/addon-knobs';
import { ComponentHeader, Tabs, CssVars } from 'storybook/blocks';

import { Spinner } from '../Component';
import { name, version } from '../../package.json';
import Description from './description.mdx';
import Changelog from '../../CHANGELOG.md';
import styles from '!!raw-loader!../index.module.css';


<Meta
    title='Компоненты/Spinner'
    component={Spinner}
/>


<!-- Canvas -->

<Story name='Spinner'>
    <Spinner
        size={ select('size', ['s', 'm']) }
        visible={ boolean('visible', true) }
    />
</Story>


<!-- Docs -->

<ComponentHeader
    name='Spinner'
    version={version}
    package='@alfalab/core-components/spinner'
    stage={2}
/>

```tsx
import { Spinner } from '@alfalab/core-components/spinner';
```

<Tabs
    description={<Description />}
    props={<Props of={Spinner} />}
    cssVars={<CssVars css={styles} />}
    changelog={<Changelog />}
/>
