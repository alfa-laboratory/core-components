import { Meta, Story, Props, Title } from '@storybook/addon-docs/blocks';
import { text, select, boolean } from '@storybook/addon-knobs';
import { Status } from 'storybook/blocks/status';
import { Design } from 'storybook/blocks/design';

import { PureInput } from './Component';
import { name, version } from '../package.json';


<Meta
    title='Компоненты'
    component={PureInput}
    parameters={{ 'theme-switcher': { themes: ['click'] } }}
/>

<Title>
    PureInput ({name}@{version})
</Title>

<Status>
  Candidate Recommendation
</Status>

```tsx
import { PureInput } from '@alfalab/core-components-pure-input';
```

<Design>
  <a href="https://www.figma.com/file/hqSP3L6qu8UcL3sf18Su1M/Web-Components?node-id=49%3A0">Figma</a>
</Design>

Компонент, представляющий из себя стилизованный нативный `input`

<Story name='PureInput'>
    <PureInput
        size={select('size', ['s', 'm', 'l'], 's')}
        type={select(
            'type',
            ['number', 'card', 'email', 'money', 'password', 'tel', 'text'],
            'text',
        )}
        block={boolean('block', false)}
        disabled={boolean('disabled', false)}
        placeholder={text('placeholder', '')}
    />
</Story>

<Props of={PureInput} />
