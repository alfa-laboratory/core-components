import { Meta, Story, Props } from '@storybook/addon-docs';
import { text, select, boolean } from '@storybook/addon-knobs';
import { ComponentHeader, Tabs, CssVars } from 'storybook/blocks';

import { FilterTag } from '../Component';
import { version } from '../../package.json';
import Description from './description.mdx';
import Changelog from '../../CHANGELOG.md';
import styles from '!!raw-loader!../index.module.css';

export const SIZES = ['xxs', 'xs', 's'];

<Meta
    title='Компоненты/FilterTag'
    component={FilterTag}
/>

<!-- Canvas -->

<Story name='FilterTag'>
    <div style={{
        padding: '8px',
        position: 'absolute',
        top: 0,
        left: 0,
        right: 0,
        bottom: 0,
    }}>
        <FilterTag
            size={select('size', SIZES, 's')}
            variant={select('variant',['default', 'alt'], 'default')}
            disabled={boolean('disabled', false)}
            checked={boolean('checked', false)}
            open={boolean('open', false)}
        >
            {text('children', 'FilterTag')}
        </FilterTag>
    </div>
</Story>

<!-- Docs -->

<ComponentHeader
    name='FilterTag'
    version={version}
    package='@alfalab/core-components/filter-tag'
    stage={1}
    design='https://www.figma.com/file/hqSP3L6qu8UcL3sf18Su1M/Web-Components?node-id=214%3A1873'
/>

```tsx
import { FilterTag } from '@alfalab/core-components/filter-tag';
```

<Tabs
    description={<Description />}
    props={<Props of={FilterTag} />}
    cssVars={<CssVars css={styles} />}
    changelog={<Changelog />}
/>
