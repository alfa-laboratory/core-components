import { Meta, Story, Props } from '@storybook/addon-docs';
import { select, text } from '@storybook/addon-knobs';
import { ComponentHeader, Tabs } from 'storybook/blocks';

import { Status, colors } from '../Component';
import { name, version } from '../../package.json';
import Description from './description.mdx';
import Changelog from '../../CHANGELOG.md';


<Meta
    title='Компоненты/Status'
    component={Status}
/>


<!-- Canvas -->

<Story name='Status'>
    <Status
        color={select('color', colors, 'green')}
        view={select('view', ['soft', 'contrast'], 'soft')}
    >
        {text('children', 'Label')}
    </Status>
</Story>


<!-- Docs -->

<ComponentHeader
    name='Status'
    version={version}
    package='@alfalab/core-components/status'
    stage={1}
/>

```tsx
import { Status } from '@alfalab/core-components/status';
```

<Tabs
    description={<Description />}
    changelog={<Changelog />}
    props={<Props of={Status} />}
/>
