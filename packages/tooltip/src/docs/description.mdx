Компонент `Tooltip`.
Если тултип не помещается в переданной позиции (position),
то он отобразится с противоположной стороны (если там будет место).

Можно вручную указать, в каких позициях он будет отображаться,
в случае нехватки места, через свойство `fallbackPlacements`.

```tsx live
<div
    style={{
        width: '100%',
        height: '300px',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
    }}
>
    <Tooltip
        position='left'
        trigger='click'
        content={
            <div style={{ width: '215px' }}>
                Теперь вам можно снимать наличные в кассе и банкоматах, если ваш тариф это позволяет
            </div>
        }
        fallbackPlacements={['bottom', 'top']}
    >
        <div style={{ padding: '15px', border: '1px dashed rgba(0, 0, 0, 0.1)' }}>
            Нажми на меня
        </div>
    </Tooltip>
</div>
```

Также возможен вид `Hint`.

```tsx live
<div
    style={{
        width: '100%',
        height: '300px',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
    }}
>
    <Tooltip content={<div>Редактировать</div>} position='left' view='hint' trigger='click'>
        <div style={{ padding: '15px', border: '1px dashed rgba(0, 0, 0, 0.1)' }}>
            Нажми на меня
        </div>
    </Tooltip>
</div>
```

Компонент `TooltipResponsive`.

Адаптивный тултип (на мобильных устройствах отобразится BottomSheet).

```tsx
import { TooltipResponsive } from '@alfalab/core-components/tooltip/responsive';
```

```tsx live
const Example = () => {
    const [open, setOpen] = React.useState(false);

    const handleOpen = () => {
        setOpen(true);
    };

    const handleClose = () => {
        setOpen(false);
    };

    return (
        <div
            style={{
                width: '100%',
                height: '300px',
                display: 'flex',
                justifyContent: 'center',
                alignItems: 'center',
            }}
        >
            <TooltipResponsive
                open={open}
                content={<div>Tooltip Responsive</div>}
                position='right'
                onOpen={handleOpen}
                onClose={handleClose}
                mobileActionButton={
                    <Button view='outlined' block={true} size='s' onClick={handleClose}>
                        Закрыть
                    </Button>
                }
                fallbackPlacements={['bottom', 'top']}
            >
                <div style={{ padding: '15px', border: '1px dashed rgba(0, 0, 0, 0.1)' }}>
                    Подробнее
                </div>
            </TooltipResponsive>
        </div>
    );
};

render(<Example />);
```
