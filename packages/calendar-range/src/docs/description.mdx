Компонент для ввода даты.

```jsx live
// import { startOfDay, subMonths, addMonths, startOfMonth, startOfDay } from 'date-fns';
// import { parseDateString } from '@alfalab/core-components/calendar-input';

render(() => {
    const [calendarPosition, setCalendarPosition] = React.useState('static');

    const defaultDate = parseDateString('10.03.2022');
    const [valueFrom, setValueFrom] = React.useState('05.03.2022');
    const [valueTo, setValueTo] = React.useState('09.03.2022');

    const minDate = subMonths(startOfDay(defaultDate), 3).getTime();
    const maxDate = addMonths(startOfDay(defaultDate), 3).getTime();

    return (
        <Space size='l'>
            <RadioGroup
                label='calendarPosition'
                direction='horizontal'
                onChange={(_, { value }) => setCalendarPosition(value)}
                value={calendarPosition}
            >
                <Radio label='static' value='static' />
                <Radio label='popover' value='popover' />
            </RadioGroup>

            <CalendarRange
                valueFrom={valueFrom}
                valueTo={valueTo}
                minDate={minDate}
                maxDate={maxDate}
                defaultMonth={startOfMonth(defaultDate)}
                offDays={[startOfDay(defaultDate)]}
                calendarPosition={calendarPosition}
                onDateFromChange={({ value }) => setValueFrom(value)}
                onDateToChange={({ value }) => setValueTo(value)}
            />

            <span>
                Values: {valueFrom} — {valueTo}
            </span>

            <Space direction='horizontal'>
                <Button size='xxs' type='button' onClick={() => setValueFrom('')}>
                    Сбросить начало
                </Button>
                <Button size='xxs' type='button' onClick={() => setValueTo('')}>
                    Сбросить конец
                </Button>

                <Button size='xxs' type='button' onClick={() => setValueFrom('20.02.2022')}>
                    Начало 20.02.2022
                </Button>
                <Button size='xxs' type='button' onClick={() => setValueTo('25.04.2022')}>
                    Конец 25.04.2022
                </Button>
            </Space>
        </Space>
    );
});
```
