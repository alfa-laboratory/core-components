import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import { text, select, boolean } from '@storybook/addon-knobs';
import { CssVars, ComponentHeader, Tabs } from 'storybook/blocks';

import { Tag } from '../';
import { name, version } from '../../package.json';
import styles from '!!raw-loader!../index.module.css';
import Description from './description.mdx';

export const SIZES = ['xs', 's', 'm', 'l', 'xl'];


<Meta
    title='Компоненты'
    component={Tag}
    parameters={{ 'theme-switcher': { themes: ['click', 'mobile'] } }}
/>


<!-- Canvas -->

<Story name='Tag'>
    {React.createElement(() => {
        const [checked, setChecked] = React.useState(false);
        const handleClick = () => setChecked(!checked);
        return (
            <Tag
                size={select('size', SIZES, 'm')}
                disabled={boolean('disabled', false)}
                checked={checked || boolean('checked', false)}
                rightAddons={boolean('rightAddons', false) && <Icon />}
                onClick={handleClick}
            >
                {text('label', 'Оплатить')}
            </Tag>
        );
    })}
</Story>


<!-- Docs -->

<ComponentHeader
    name='Tag'
    version={version}
    package='@alfalab/core-components-tag'
    stage={3}
    design='https://www.figma.com/file/hqSP3L6qu8UcL3sf18Su1M/Web-Components?node-id=214%3A1873'
/>

```tsx
import { Tag } from '@alfalab/core-components-tag';
```

<Tabs
    description={<Description />}
    prpos={<Props of={Tag} />}
    cssVars={<CssVars css={styles} />}
/>
