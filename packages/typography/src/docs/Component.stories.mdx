import { Meta, Story, Props } from '@storybook/addon-docs';
import { text, boolean, select, number } from '@storybook/addon-knobs';
import { ComponentHeader, Tabs } from 'storybook/blocks';

import { Typography } from '../component';
import { name, version } from '../../package.json';
import Description from './description.mdx';
import Changelog from '../../CHANGELOG.md';
import { colors } from '../colors';


<Meta title='Компоненты/Typography' component={Typography} />


<!-- Canvas -->

<Story name='Typography.Title'>
    {React.createElement(() => {
        const VIEW_TYPES = [
            'xlarge',
            'large',
            'medium',
            'small',
            'xsmall',
        ];
        const color = select('color', colors, '');
        const weight = select('weight', ['regular', 'medium', 'bold'], 'medium');
        const font = select('font', ['styrene', 'system'], 'styrene');
        return VIEW_TYPES.map(view => (
            <Typography.Title
                defaultMargins={boolean('defaultMargins', false)}
                view={view}
                color={color}
                weight={weight}
                font={font}
                key={view}
            >
                заголовок view='{view}'
            </Typography.Title>
        ))
    })}
</Story>

<Story name='Typography.TitleResponsive'>
    {React.createElement(() => {
        const VIEW_TYPES = [
            'xlarge',
            'large',
            'medium',
            'small',
            'xsmall',
        ];
        const color = select('color', colors, '');
        const weight = select('weight', ['regular', 'medium', 'bold'], 'medium');
        const font = select('font', ['styrene', 'system'], 'styrene');
        return VIEW_TYPES.map(view => (
            <Typography.TitleResponsive
                defaultMargins={boolean('defaultMargins', false)}
                view={view}
                color={color}
                weight={weight}
                font={font}
                key={view}
            >
                заголовок view='{view}'
            </Typography.TitleResponsive>
        ))
    })}
</Story>

<Story name='Typography.TitleMobile'>
    {React.createElement(() => {
        const VIEW_TYPES = [
            'xlarge',
            'large',
            'medium',
            'small',
            'xsmall',
        ];
        const color = select('color', colors, '');
        const weight = select('weight', ['regular', 'medium', 'bold', '-'], '-');
        const font = select('font', ['styrene', 'system'], 'styrene');
        return VIEW_TYPES.map(view => (
            <Typography.TitleMobile
                view={view}
                color={color}
                weight={weight}
                font={font}
                key={view}
            >
                заголовок view='{view}'
            </Typography.TitleMobile>
        ))
    })}
</Story>

<Story name='Typography.Text'>
    {React.createElement(() => {
        const VIEW_TYPES = [
            'primary-large',
            'primary-medium',
            'primary-small',
            'secondary-large',
            'secondary-medium',
            'secondary-small',
            'component',
            'caps',
        ];
        const color = select('color', colors, '');
        const tag = select('tag', ['div', 'p', 'span'], 'p');
        const weight = select('weight', ['regular', 'medium', 'bold'], 'regular');
        const monospace = boolean('monospaceNumbers', false);
        return VIEW_TYPES.map(view => (
            <Typography.Text view={view} color={color} tag={tag} weight={weight} monospaceNumbers={monospace} key={view}>
                {view}. Космологи́ческая сингуля́рность — состояния Вселенной в определённый момент времени в прошлом,
                когда плотность энергии (материи) и кривизна пространства-времени были очень велики — порядка
                планковских значений. 1234567890, например.
            </Typography.Text>
        ));
    })}
</Story>


<!-- Docs -->

<ComponentHeader
    name='Typography'
    version={version}
    package='@alfalab/core-components/typography'
    stage={3}
    design='https://www.figma.com/file/FWya6P55TnXkiVGaM2qBio/Web-Hybrid-Typography'
/>

```tsx
import { Typography } from '@alfalab/core-components/typography';
```

<Tabs
    description={<Description />}
    changelog={<Changelog />}
    props={<Props components={Typography} />}
/>
