Набор компонентов для оформления текста, реализующий [гибридную систему типографики](https://www.figma.com/file/FWya6P55TnXkiVGaM2qBio/2020-Web-Hybrid-Typography?node-id=593%3A0).

Для интерактивных элементов, требующих изменение цвета при наведении, следует устанавливать цвет через CSS. Пропса `color` — для статичных элементов.

> **_NOTE:_** Компоненты не устанавливают свойство `font-family`, кроме случаев, когда используется шрифт 'Styrene UI'. Подразумевается, что шрифт будет установлен у родителя.

[Подробнее про использование типографики](https://alfa-laboratory.github.io/core-components/master/?path=/docs/гайдлайны-типографика--page)

## Title

### Styrene

```tsx live
render(() => {
    const VIEW_TYPES = ['xlarge', 'large', 'medium', 'small', 'xsmall'];

    return VIEW_TYPES.map(view => (
        <Typography.Title view={view} color={color} key={view} tag='div'>
            {view}
        </Typography.Title>
    ));
});
```
### System

```tsx live
render(() => {
    const VIEW_TYPES = ['xlarge', 'large', 'medium', 'small', 'xsmall'];

    return VIEW_TYPES.map(view => (
        <Typography.Title font="system" view={view} color={color} key={view} tag='div'>
            {view}
        </Typography.Title>
    ));
});
```
## TitleMobile

### Styrene

```tsx live
render(() => {
    const VIEW_TYPES = ['xlarge', 'large', 'medium', 'small', 'xsmall'];

    return VIEW_TYPES.map(view => (
        <Typography.TitleMobile view={view} key={view} tag='div'>
            {view}
        </Typography.TitleMobile>
    ));
});
```
### System

```tsx live
render(() => {
    const VIEW_TYPES = ['xlarge', 'large', 'medium', 'small', 'xsmall'];

    return VIEW_TYPES.map(view => (
        <Typography.TitleMobile view={view} font="system" key={view} tag='div'>
            {view}
        </Typography.TitleMobile>
    ));
});
```
## TitleResponsive

До 600рх mobile, после 600px Desktop 

### Styrene

```tsx live
render(() => {
    const VIEW_TYPES = ['xlarge', 'large', 'medium', 'small', 'xsmall'];

    return VIEW_TYPES.map(view => (
        <Typography.TitleResponsive view={view} key={view} tag='div'>
            {view}
        </Typography.TitleResponsive>
    ));
});
```
### System

```tsx live
render(() => {
    const VIEW_TYPES = ['xlarge', 'large', 'medium', 'small', 'xsmall'];

    return VIEW_TYPES.map(view => (
        <Typography.TitleResponsive font="system" view={view} key={view} tag='div'>
            {view}
        </Typography.TitleResponsive>
    ));
});
```
## Text

### System

Компонент для оформления текста. Позволяет задать внешний вид, тэг и цвет текста.

```tsx live
render(() => {
    const VIEW_TYPES = [
        'primary-large',
        'primary-medium',
        'primary-small',
        'secondary-large',
        'secondary-medium',
        'secondary-small',
        'component',
        'caps',
    ];
    return VIEW_TYPES.map(view => (
        <Typography.Text view={view} key={view} tag='p'>
            {view}. Космологи́ческая сингуля́рность — состояния Вселенной в определённый момент
            времени в прошлом, когда плотность энергии (материи) и кривизна пространства-времени
            были очень велики — порядка планковских значений.
        </Typography.Text>
    ));
});
```
