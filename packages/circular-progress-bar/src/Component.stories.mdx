import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import { text, select, number } from '@storybook/addon-knobs';
import { ComponentHeader } from 'storybook/blocks/component-header';

import { name, version } from '../package.json';
import { CircularProgressBar } from './Component';


<Meta title='Компоненты' component={CircularProgressBar} />


<!-- Canvas -->

<Story name='CircularProgressBar'>
    {React.createElement(() => {
        const value = number('value', 50);
        const view = select('view', ['positive', 'negative']);
        const title = text('title', 'title');
        const subtitle = text('subtitle', 'subtitle');
        return (
            <React.Fragment>
                <CircularProgressBar
                    value={value}
                    view={view}
                    title={title}
                    subtitle={subtitle}
                    size={select('size', ['m', 'l'])}
                />
                <p>Без атрибута title значение для title берётся из value</p>
                <CircularProgressBar
                    value={value}
                    view={view}
                    subtitle={subtitle}
                    size='l'
                />
                <p>Если есть children, то они подставляются в центр, а title и subtitle игнорируются</p>
                <CircularProgressBar
                    value={value}
                    view={view}
                    title={title}
                    subtitle={subtitle}
                    size='l'
                >
                    <p>Hello</p>
                </CircularProgressBar>
            </React.Fragment>
        )
    })}
</Story>


<!-- Docs -->

<ComponentHeader
    name='CircularProgressBar'
    version={version}
    package='@alfalab/core-components-circular-progress-bar'
    stage={1}
/>

```tsx
import { CircularProgressBar } from '@alfalab/core-components-circular-progress-bar';
```

Отображает круговой индикатор прогресса (0-100%).

<CircularProgressBar value={40} />


<Props of={CircularProgressBar} />
