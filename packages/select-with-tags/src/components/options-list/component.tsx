import React from 'react';
import {
    OptionsList as DefaultOptionsList,
    OptionsListProps,
} from '@alfalab/core-components-select';

export const OptionsList = (props: OptionsListProps) => {
    return <DefaultOptionsList {...props} />;
};
