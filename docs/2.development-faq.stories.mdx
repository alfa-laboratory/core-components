import { Description } from '@storybook/addon-docs/blocks';

<Meta title='Принципы|FAQ' />

## 🔨 Разработка

Мы стараемся придерживаться следующих базовых принципов разработки:

- [KISS](https://ru.wikipedia.org/wiki/KISS_%28%D0%BF%D1%80%D0%B8%D0%BD%D1%86%D0%B8%D0%BF%29)
- [YAGNI](https://ru.wikipedia.org/wiki/YAGNI)
- [DRY](https://ru.wikipedia.org/wiki/Don%E2%80%99t_repeat_yourself)

Максимально простой код с низком порогом входа.

---

### Колбэки

Мы [решили](https://github.com/alfa-laboratory/core-components/issues/45#issuecomment-609640997), что колбэки в компонентах должны иметь общий вид:

```tsx
onSomeEvent(event, payload) => void;
//
onChange?: (
    event?: ChangeEvent<HTMLInputElement>,
    payload?: {
        checked: boolean;
        name: string;
    },
) => void;
```

### Обработчики событий

```tsx
/* Component.tsx */

// Good

const handleArrowClick = () => {}

const handleArrowClick = useCallback(() => {}, [])

// Bad

function onArrowClick() {}

const onArrowClick = function() {}

const onArrowClick = () => {}

```

## 📚 Документация

Документирование атрибутов React компонентов

```tsx
// Good

type InputProps = {
    /**
     * Размер компонента
     */
    size?: 's' | 'm' | 'l' | 'xl';
}

// Bad

type InputProps = {
    size?: 's' | 'm' | 'l' | 'xl';
}

```


## 💅 Темизация

Компоненты поставляются вместе с темами, которые могут быть использованы двумя способами.

### Использование нативных css-переменных

В этом случае, вы можете подключить переменные из файла темы в любом месте. Можно как изменить вид всех компонентов, так и сделать точечное изменение.

Для поддержки IE11 используйте данный [полифилл](https://github.com/nuxodin/ie11CustomProperties)

### Транспайлинг переменных при сборке

В этом случае вы сможете только переопределить все переменные. Точечная настройка и изменение темы "на лету" не будут доступны.

- Используйте [alfa-laboratory/postcss-custom-properties](https://github.com/alfa-laboratory/postcss-custom-properties), указав в `importFrom` путь к файлу с темой.

- Если вы используете [arui-scripts](https://github.com/alfa-laboratory/arui-scripts), то добавьте в `package.json`:

  ```json
  "сomponentsTheme": "./src/styles/theme.css"
  ```

