import { Description } from '@storybook/addon-docs/blocks';

<Meta title='Гайдлайны|Доступность / Что это?' />

# Что это такое?

Доступность (Accessibility) интерфейса — это возможность использования интерфейса людьми с физическими или техническими ограничениями.

Доступным сайтом или приложением должно быть возможно пользоваться, вне зависимости от:
- браузера
- устройства
- особенностей и ограничений самого пользователя

# Зачем это нужно?

Люди с ограниченными возможностями должны иметь возможность пользоваться функционалом сайтов и приложений.<br/>
Помимо этого, обычные пользователи также могут находиться в условиях, которые будут ограничивать или делать невозможным использование интерфейса:

- Возраст
- Усталость
- Плохое зрение
- Занятые руки, перчатки, управление автомобилем, велосипедом и т.д.

Но все эти люди — это клиенты, которые покупают товары и пользуются услугами, поэтому очень важно обеспечить для них удобство и доступность интерфейсов при максимально широких условиях.

Особенно это важно для банков, гос. учреждений, медицинских и страховых компаний, клиентами которых часто бывают люди с ограниченными возможностями.

# Как это сделать?

Существуют государственные рекомендации к обеспечению доступности.<br/>
В России — это [ГОСТ 52872‑2007](http://www.tehlit.ru/1lib_norma_doc/53/53197/index.htm), который предусматривает обязательную доступность сайтов для незрячих.<br/>
На западе — сайты должны соответствовать международным рекомендациям [WCAG](https://www.w3.org/WAI/standards-guidelines/wcag/) и [Section 508](https://www.section508.gov/).

## 1. Старайтесь использовать семантический HTML

HTML5 уже предоставляет широкий выбор семантических элементов. Поэтому, если это возможно, следует использовать нативные элементы или атрибуты с заложенной семантикой и поведением.

```html
❌ Плохо
<div>Click Me</div>

❌ Не очень
<div role="button">Click Me</div>

✅ Хорошо
<button type="button">Click Me</button>
```

```html
❌ Плохо
<div class="menu">
    <span class="menu-item current">Шапки, шарфы, варежки</span>
    <span class="menu-item"><a href="#">Цены</a></span>
    <span class="menu-item"><a href="#">Контакты</a></span>
</div>

✅ Хорошо
<nav>
    <ul>
        <li aria-current="page">
            <span class="current"><span class="visually-hidden">Текущий пункт:</span>Шапки, шарфы, варежки</span>
        </li>
        <li><a href="#">Цены</a></li>
        <li><a href="#">Контакты</a></li>
    </ul>
</nav>
```

## 2. Проектируйте правильную структуру страниц

Верстка страниц должна быть семантически правильной — для этого используйте HTML-элементы по их смысловому назначению.

Для заголовков используйте `<h1>`, `<h2>`, `<h3>`, `<h4>`, `<h5>`, `<h6>`. Соблюдайте четкую иерархию заголовков.

Для маркированных списков используйте `<ul>`, для нумерованных `<ol>`.

Используйте теги HTML5, либо ARIA-роли:
- `<header>` для шапки сайта. Поместите вводные элементы сайта, которые есть на каждой странице — логотип, навигация, заголовок страницы, поиск
- `<nav>` для навигации по сайту
- `<section>` для разделения страницы на смысловые части
- `<aside>` для боковой колонки со списком статей, рекламы, архивных записей
- `<footer>` для нижнего блока страницы с информацией об авторе статьи, данные о копирайте и т. д.
- `<figure>` для группировки элементов, например, картинку и текст
- `<main>` для основного содержимого страницы.

**С полным описанием тэгов и их семантическим смыслом можно ознакомиться [здесь](https://html5book.ru/html-html5/)**


## 3. Старайтесь не менять ARIA-ролями поведение семантических элементов

Каждый HTML-элемент имеет свой смысл и назначение.

Если используется `button`, то устройства понимают, что это интерактивный элемент, с которым можно взаимодействовать с помощью мыши, клавиши Enter или пробела. В случае `a` - что взаимодействие приведет к переходу на другую страницу. Не стоит менять это поведение.

```html
❌ Плохо
<h1 role="button">Clickable header</h1>

✅ Хорошо
<h1><button>Clickable header</button></h1>
```

## 4. Интерактивные элементы должны быть хорошо проработаны

Недостаточно просто добавить ARIA-роль для элемента. Например, если кнопка реализуется с помощью тэга `<div>`, а не `<button>`, то следует вручную реализовать такое же поведение, как у нативной кнопки.

В руководстве [ARIA](https://www.w3.org/TR/2019/NOTE-wai-aria-practices-1.1-20190814/examples/) указан список возможностей и поведение, которым должен обладать каждый элемент.<br/>
Создание элемента, выглядящего как кнопка, не делает его кнопкой. Правильная кнопка должна удовлетворять следующим требованиям:

- Должна быть кликабельной с помощью курсора
- Должна быть кликабельной с помощью клавиши Enter
- Должна быть кликабельной с помощью клавиши пробела

## 5. Описывайте интерактивные элементы

Элементы, с которыми можно взаимодействовать, например кнопки и ссылки, должны иметь описание, позволяющее понять, что эти элементы делают.

```html
❌ Плохо
<input type="text" placeholder="Имя пользователя">


✅ Хорошо
<label>
  Имя пользователя
  <input type="text">
</label>

✅ Хорошо
<label for="password">Пароль</label>
<input type="password" id="password">

✅ Хорошо
<button type="button">Загрузить историю платежей</button>
```

---

**В следующем разделе будут приведены практические рекомендации по разработке доступных интерфейсов.**
